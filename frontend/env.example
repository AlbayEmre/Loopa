# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000

# Firebase Configuration (Client Side)
# Get these from Firebase Console > Project Settings > General > Your apps > Web app
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:abcdef
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# IMPORTANT SECURITY NOTES:
# 1. NEVER commit the actual .env.local file to Git
# 2. These are client-side variables (NEXT_PUBLIC_*) and will be exposed to the browser
# 3. Use Firebase Security Rules to protect your data
# 4. For production, set these in Vercel environment variables
